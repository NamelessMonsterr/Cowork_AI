# Flash Assistant - Environment Variables Configuration

# ============================================================================
# PRODUCTION SECURITY SETTINGS (REQUIRED)
# ============================================================================

# Session Secret Key - MUST be set in production
# Generate with: openssl rand -hex 32
FLASH_SESSION_SECRET=your-secure-random-secret-here

# Environment Mode - Set to "production" for secure defaults
# Values: "production" | "development" | "" (empty = dev)
ENV=production

# ============================================================================
# DEVELOPMENT/DEBUG SETTINGS (OPTIONAL - Default: disabled)
# ============================================================================

# Enable Debug/Dev Endpoints - NEVER enable in production!
# Enables: /debug/crash, /dev/run, /voice/dev_simulate, /debug/type, /debug/open_app
# Values: "true" | "false"
# Default: false
# FLASH_DEV_ENDPOINTS_ENABLED=false

# Skip Platform Check - For testing on non-Windows systems
# Values: "true" | "false"  
# Default: false
# COWORK_SKIP_PLATFORM_CHECK=false

# ============================================================================
# API KEYS (OPTIONAL)
# ============================================================================

# OpenAI API Key - For voice transcription and planning
# OPENAI_API_KEY=sk-your-openai-key-here

# ============================================================================
# DEPLOYMENT EXAMPLE
# ============================================================================

# Production deployment should look like:
# export FLASH_SESSION_SECRET="$(openssl rand -hex 32)"
# export ENV="production"
# python run_backend.py

# Development deployment can use defaults:
# python run_backend.py
# (Session secret auto-generated with warning)